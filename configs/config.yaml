defaults:
  - data: default
  - model: two_stream
  - training: two_stage
  - inference: default
  - augmentation: default
  - _self_

# Global settings
seed: 42
device: cuda

# Output directories
output_dir: outputs
checkpoint_dir: checkpoints

# Hydra configuration
hydra:
  run:
    dir: ${output_dir}/${now:%Y-%m-%d_%H-%M-%S}
