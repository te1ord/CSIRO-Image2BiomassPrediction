# Example: Tuned model configuration
# Usage: python scripts/train.py model=dinov2_lasso_tuned

# @package _global_

dinov2:
  # Path to pretrained DINOv2 model
  model_path: /kaggle/input/dinov2/pytorch/base/1/
  processor_path: /kaggle/input/dinov2/pytorch/base/1/

lasso:
  alpha: 0.5  # Reduced regularization for potentially better fit
  n_targets: 5
  n_folds: 5

# Model output
save_path: models/lasso_ensemble_tuned.pkl

