# DINOv2 Two-Stream Tiled FiLM Model Configuration
# High-performance variant using DINOv2 backbone with FiLM modulation

model_type: two_stream

# Backbone configuration (DINOv2 from timm)
backbone:
  name: timm/vit_large_patch14_dinov2.lvd142m #vit_base_patch14_dinov2
  pretrained: true

# Head configuration
heads:
  dropout: 0.30
  hidden_ratio: 0.25

# Tiled model settings
tiled:
  grid: [2, 2]

# Checkpoint
save_path: ${checkpoint_dir}
