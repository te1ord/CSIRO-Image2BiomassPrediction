defaults:
  - data: default
  - model: two_stream_tiled_film_dinov2large
  - training: mlps_only
  - inference: default
  - augmentation: default
  - logging: wandb
  - _self_

# Global settings
seed: 42
device: cuda

exp_name: our_split_mlps_only_dinov2large_17_23_feature_layers_cls_mean_pooling_w_semantic_features_vision_only_wembed_mixup_adaptive_unfreeze

# Output directories
output_dir: outputs
checkpoint_dir: checkpoints/${logging.experiment_name}

# Hydra configuration
hydra:
  run:
    dir: ${output_dir}/${now:%Y-%m-%d_%H-%M-%S}
 