defaults:
  - data: default
  - model: two_stream_tiled_dinov3huge
  - training: mlps_only
  - inference: default
  - augmentation: default
  - logging: wandb
  - _self_

# Global settings
seed: 42
device: cuda

exp_name: our_split_mlps_only_dinov3large_last_layer_feature_layers_cls_mean_pooling_wembed_mixup

# Output directories
output_dir: outputs
checkpoint_dir: checkpoints/${logging.experiment_name}

# Hydra configuration
hydra:
  run:
    dir: ${output_dir}/${now:%Y-%m-%d_%H-%M-%S}
 